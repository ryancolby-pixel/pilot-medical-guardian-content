.DS_Store
__pycache__/
*.pyc
.claude/

# press.html is UNFINISHED — it still contains visible "[PLACEHOLDER: App Store URL]"
# text and 404s on the live site. Ignored so a careless `git add -A` can't publish a
# placeholder page under our own domain. It stays on disk, editable and previewable
# locally; DELETE THIS LINE when the real copy and the App Store URL are in place.
press.html

# Generated by scripts/check-source-links.py during CI. Never publish these:
# the Pages deploy stages the repo root with rsync, so an untracked report file
# here would end up served at pilotmedicalguardian.com/source-link-report.md
source-link-report.md
link-output.txt
link-comment.md
drift-comment.md
